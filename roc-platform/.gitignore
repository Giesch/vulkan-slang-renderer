# Build artifacts
target/
generated-docs/

# The host archive, rebuilt by build.sh
platform/targets/*/libhost.a

# Executables built by `roc build examples/<name>.roc`
/basic_triangle

# The floor-assertion probe ci/all_tests.sh writes beside generate.sh
stubs/floor_probe.*

# Roc build artifacts
*.rh
*.rm
*.wasm
*.ll

# Archives (platform bundles)
*.tar.br
*.tar.gz
*.tar.zst

# OS files
.DS_Store
