# rust logging configuration
# renderer::debug includes vulkan debug_utils callback setup
RUST_LOG=mltrs_renderer::renderer::debug=warn,mltrs_renderer::renderer=info

# Wind Waker checkout for toon_link asset extraction
TWW_DIR=/path/to/tww
