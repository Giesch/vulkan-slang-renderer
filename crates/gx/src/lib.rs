//! GX (GameCube GPU) emulation support for the `toon_link` example.
//!
//! The manifest schema for converted assets, shared between the
//! `convert_link` asset converter and the example that renders its output.
//! The TEV uniform packing lives with the example itself (it depends on the
//! example's generated shader bindings).

pub mod model_manifest;
