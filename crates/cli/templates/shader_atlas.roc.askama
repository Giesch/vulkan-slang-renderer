{% for entry in modules -%}
import {{ entry.module }}
{% endfor -%}
{% if !modules.is_empty() %}
{% endif -%}
## Generated; directories are tool-owned.
ShaderAtlas := {}.{
{%- for entry in modules %}
{%- if !loop.first %}
{% endif %}
    {{ entry.value }} = {
        reflection: {{ entry.module }}.reflection,
        stages: {{ entry.module }}.stages,
    }
{%- endfor %}
}

